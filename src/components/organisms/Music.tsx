import React from "react";
import SectionHeader from "../atoms/SectionHeader";

const Music = () => {
  return (
    <section className="max-w-[1200px] mx-auto px-[1rem] sm:px-[2rem] lg:px-[3rem] my-[0rem] ">
      <SectionHeader padLeft={0} text="MY MUSIC" />
    </section>
  );
};

export default Music;
